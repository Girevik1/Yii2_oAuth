#-----------------------------------------------#
# ������ ����� ������������ HTTP �����
#-----------------------------------------------#


<VirtualHost *:%httpport%>

    DocumentRoot    "%hostdir%"
    ServerName      "%host%"
    ServerAlias     "%host%" %aliases%
    ScriptAlias     /cgi-bin/ "%hostdir%/cgi-bin/"

</VirtualHost>
 <VirtualHost *:80>
     ServerName zf2-tutorial.localhost
     DocumentRoot C:/Users/user/Desktop/PhP\OSPanel/domains/zf2-tutorial.localhost/public
     SetEnv APPLICATION_ENV "development"
     <Directory C:/Users/user/Desktop/PhP\OSPanel/domains/zf2-tutorial.localhost/public>
         DirectoryIndex index.php
         AllowOverride All
         Require all granted
     </Directory>
 </VirtualHost>

<VirtualHost *:443>
        ServerName yii-oauth.com
        DocumentRoot "C:/Users/user/Desktop/PhP\OSPanel/domains/yii-oauth.com/frontend/web/"
        
        <Directory "C:/Users/user/Desktop/PhP\OSPanel/domains/yii-oauth.com/frontend/web/">
            # use mod_rewrite for pretty URL support
            RewriteEngine on
            # If a directory or a file exists, use the request directly
            RewriteCond %{REQUEST_FILENAME} !-f
            RewriteCond %{REQUEST_FILENAME} !-d
            # Otherwise forward the request to index.php
            RewriteRule . index.php

            # use index.php as index file
            DirectoryIndex index.php

            # ...other settings...
            # Apache 2.4
            Require all granted
            
            ## Apache 2.2
            # Order allow,deny
            # Allow from all
        </Directory>
    </VirtualHost>
    
    <VirtualHost *:443>
        ServerName yii-oauth-admin.com
        DocumentRoot "C:/Users/user/Desktop/PhP\OSPanel/domains/yii-oauth.com/backend/web/"
        
        <Directory "C:/Users/user/Desktop/PhP\OSPanel/domains/yii-oauth.com/backend/web/">
            # use mod_rewrite for pretty URL support
            RewriteEngine on
            # If a directory or a file exists, use the request directly
            RewriteCond %{REQUEST_FILENAME} !-f
            RewriteCond %{REQUEST_FILENAME} !-d
            # Otherwise forward the request to index.php
            RewriteRule . index.php

            # use index.php as index file
            DirectoryIndex index.php

            # ...other settings...
            # Apache 2.4
            Require all granted
            
            ## Apache 2.2
            # Order allow,deny
            # Allow from all
        </Directory>
    </VirtualHost>

<VirtualHost *:80>
        ServerName yii-test.com
        DocumentRoot "C:/Users/user/Desktop/PhP\OSPanel/domains/yii-test.com/frontend/web/"
        
        <Directory "C:/Users/user/Desktop/PhP\OSPanel/domains/yii-test.com/frontend/web/">
            # use mod_rewrite for pretty URL support
            RewriteEngine on
            # If a directory or a file exists, use the request directly
            RewriteCond %{REQUEST_FILENAME} !-f
            RewriteCond %{REQUEST_FILENAME} !-d
            # Otherwise forward the request to index.php
            RewriteRule . index.php

            # use index.php as index file
            DirectoryIndex index.php

            # ...other settings...
            # Apache 2.4
            Require all granted
            
            ## Apache 2.2
            # Order allow,deny
            # Allow from all
        </Directory>
    </VirtualHost>
    
    <VirtualHost *:80>
        ServerName yii-test.com
        DocumentRoot "C:/Users/user/Desktop/PhP\OSPanel/domains/yii-admin.com/backend/web/"
        
        <Directory "C:/Users/user/Desktop/PhP\OSPanel/domains/yii-admin.com/backend/web/">
            # use mod_rewrite for pretty URL support
            RewriteEngine on
            # If a directory or a file exists, use the request directly
            RewriteCond %{REQUEST_FILENAME} !-f
            RewriteCond %{REQUEST_FILENAME} !-d
            # Otherwise forward the request to index.php
            RewriteRule . index.php

            # use index.php as index file
            DirectoryIndex index.php

            # ...other settings...
            # Apache 2.4
            Require all granted
            
            ## Apache 2.2
            # Order allow,deny
            # Allow from all
        </Directory>
    </VirtualHost>

#-----------------------------------------------#
# ����� ����� ������������ HTTP �����
#-----------------------------------------------#

#-----------------------------------------------#
# ������ ����� ������������ HTTPS �����
#-----------------------------------------------#

<IfModule ssl_module>
<VirtualHost *:%httpsport%>

    DocumentRoot    "%hostdir%"
    ServerName      "%host%"
    ServerAlias     "%host%" %aliases%
    ScriptAlias     /cgi-bin/ "%hostdir%/cgi-bin/"

    SSLEngine       on
    #Header always set          Strict-Transport-Security "max-age=94608000"

    #SSLCACertificateFile       ""
    #SSLCertificateChainFile    ""

    SSLCertificateFile          "%sprogdir%/userdata/config/cert_files/server.crt"
    SSLCertificateKeyFile       "%sprogdir%/userdata/config/cert_files/server.key"

    SetEnvIf User-Agent ".*MSIE [1-5].*" \
    nokeepalive ssl-unclean-shutdown \
    downgrade-1.0 force-response-1.0

    SetEnvIf User-Agent ".*MSIE [6-9].*" \
    ssl-unclean-shutdown

    <FilesMatch "\.(cgi|shtml|phtml|php)$">
        SSLOptions              +StdEnvVars
    </FilesMatch>

    <Directory "%hostdir%/cgi-bin/">
        SSLOptions              +StdEnvVars
    </Directory>

</VirtualHost>
</IfModule>

#-----------------------------------------------#
# ����� ����� ������������ HTTPS �����
#-----------------------------------------------#

